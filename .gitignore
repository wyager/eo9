# Build outputs of the three Cargo workspaces.
/target
/guest/target
/kernel/target

# Editor / OS noise.
*.rs.bk
.DS_Store
.claude

# vendored crates are [patch] sources, never built in place except for standalone checks
kernel/vendor/**/target/
.fork-staging/
