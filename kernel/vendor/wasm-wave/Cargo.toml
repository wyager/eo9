# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
rust-version = "1.85.0"
name = "wasm-wave"
version = "0.250.0"
authors = ["lann.martin@fermyon.com"]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "WebAssembly Value Encoding"
documentation = "https://docs.rs/wasm-wave"
readme = "README.md"
categories = [
    "wasm",
    "encoding",
    "parser-implementations",
]
license = "Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT"
repository = "https://github.com/bytecodealliance/wasm-tools/tree/main/crates/wasm-wave"

# Vendored copy of wasm-wave 0.250.0 for the kernel's no_std component algebra (plan/12).
# The crate is already `#![no_std]`; the only change is that the `wit` feature no longer
# force-enables `std` (it gated `wit` to std purely by convention). `std` now forwards
# `wit-parser?/std` instead, and the wit-parser dep is `default-features = false` so the
# kernel build stays no_std. Host builds (which enable `std`) are unchanged.
[features]
default = [
    "std",
    "wit",
]
std = ["wit-parser?/std"]
wit = ["dep:wit-parser"]

[lib]
name = "wasm_wave"
path = "src/lib.rs"

[[test]]
name = "nan"
path = "tests/nan.rs"

[[test]]
name = "ui"
path = "tests/ui.rs"

[dependencies.logos]
version = "0.14.2"
features = [
    "export_derive",
    "forbid_unsafe",
]
default-features = false

[dependencies.thiserror]
version = "2.0.12"
default-features = false

[dependencies.wit-parser]
version = "0.250.0"
optional = true
default-features = false

[dev-dependencies.anyhow]
version = "1.0.58"
default-features = false

[lints.clippy]
clone_on_copy = "warn"
manual_strip = "warn"
map_clone = "warn"
uninlined_format_args = "warn"
unnecessary_cast = "warn"
unnecessary_fallible_conversions = "warn"
unnecessary_mut_passed = "warn"
unnecessary_to_owned = "warn"

[lints.clippy.all]
level = "allow"
priority = -1

[lints.rust]
deprecated-safe-2024 = "warn"
keyword_idents_2024 = "warn"
missing-unsafe-on-extern = "warn"
rust-2024-guarded-string-incompatible-syntax = "warn"
rust-2024-incompatible-pat = "warn"
rust-2024-prelude-collisions = "warn"
unsafe-attr-outside-unsafe = "warn"
unsafe-op-in-unsafe-fn = "warn"
unsafe_code = "deny"
unstable_features = "warn"
unused-lifetimes = "warn"
unused-macro-rules = "warn"
unused_extern_crates = "warn"
unused_import_braces = "warn"

[lints.rust.unexpected_cfgs]
level = "warn"
priority = 0
check-cfg = [
    "cfg(fuzzing)",
    "cfg(debug_check_try_op)",
]
