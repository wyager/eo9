//! A/B harness for the first-poll-inline prototype — see Cargo.toml and
//! docs/spikes/first-poll-inline.md. The library target is empty; everything lives in
//! the test binaries under `tests/`, which include the pinned suites from
//! `tests/eo9-integration/tests` by `#[path]`.
