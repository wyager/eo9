# Same pinned nightly as the kernel workspace (plan/01), with the bare-metal target.
[toolchain]
channel = "nightly-2026-05-25"
profile = "minimal"
components = ["rustfmt", "clippy"]
targets = ["aarch64-unknown-none"]
