# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
rust-version = "1.93.0"
name = "wasmtime-internal-unwinder"
version = "45.0.0"
authors = ["The Wasmtime Project Developers"]
build = "build.rs"
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "INTERNAL: Wasmtime's unwind format and unwinder"
documentation = "https://docs.rs/wasmtime-unwinder"
readme = "README.md"
keywords = [
    "unwind",
    "exceptions",
]
categories = ["no-std"]
license = "Apache-2.0 WITH LLVM-exception"
repository = "https://github.com/bytecodealliance/wasmtime"
resolver = "2"

[features]
cranelift = ["dep:cranelift-codegen"]
default = []

[lib]
name = "wasmtime_internal_unwinder"
path = "src/lib.rs"

[dependencies.cfg-if]
version = "1.0"

# no_std (plan/12 D28): drop the hardcoded `std` (see cranelift-frontend for why). `core`
# is supplied through feature unification by wasmtime-cranelift's codegen dependency.
[dependencies.cranelift-codegen]
version = "0.132.0"
features = [
    "unwind",
]
optional = true
default-features = false

[dependencies.log]
version = "0.4.28"
default-features = false

[dependencies.object]
version = "0.39.0"
features = [
    "read_core",
    "elf",
]
default-features = false

[dependencies.wasmtime-environ]
version = "=45.0.0"

[lints.clippy]
allow_attributes_without_reason = "warn"
clone_on_copy = "warn"
extra_unused_type_parameters = "warn"
from_over_into = "warn"
manual_strip = "warn"
map_clone = "warn"
multiple_bound_locations = "warn"
redundant_field_names = "warn"
uninlined_format_args = "warn"
unnecessary_cast = "warn"
unnecessary_fallible_conversions = "warn"
unnecessary_mut_passed = "warn"
unnecessary_to_owned = "warn"
useless_conversion = "warn"

[lints.clippy.all]
level = "allow"
priority = -1

[lints.rust]
trivial_numeric_casts = "warn"
unstable_features = "warn"
unused-lifetimes = "warn"
unused-macro-rules = "warn"
unused_extern_crates = "warn"
unused_import_braces = "warn"

[lints.rust.unexpected_cfgs]
level = "warn"
priority = 0
check-cfg = [
    "cfg(pulley_tail_calls)",
    "cfg(pulley_assume_llvm_makes_tail_calls)",
    "cfg(pulley_disable_interp_simd)",
    "cfg(arc_try_new)",
    "cfg(fuzzing)",
    "cfg(gc_zeal)",
]
