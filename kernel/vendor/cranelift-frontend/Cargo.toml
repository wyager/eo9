# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
rust-version = "1.93.0"
name = "cranelift-frontend"
version = "0.132.0"
authors = ["The Cranelift Project Developers"]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Cranelift IR builder helper"
documentation = "https://docs.rs/cranelift-frontend"
readme = "README.md"
categories = ["no-std"]
license = "Apache-2.0 WITH LLVM-exception"
repository = "https://github.com/bytecodealliance/wasmtime"
resolver = "2"

[features]
core = [
    "hashbrown",
    "cranelift-codegen/core",
]
default = ["std"]
std = ["cranelift-codegen/std"]

[lib]
name = "cranelift_frontend"
path = "src/lib.rs"

# no_std (plan/12 D28): request the codegen `core` profile, not `std`. Upstream hardcodes
# `std` here, which (via feature unification) drags cranelift-codegen/std — and with it
# gimli/std + cranelift-control/fuzz → arbitrary — into the whole graph, breaking the
# no_std kernel build. This crate is `#![no_std]` already.
[dependencies.cranelift-codegen]
version = "0.132.0"
features = [
    "core",
    "unwind",
]
default-features = false

[dependencies.hashbrown]
version = "0.17"
optional = true
default-features = false

[dependencies.log]
version = "0.4.28"
default-features = false

[dependencies.smallvec]
version = "1.15.1"
features = ["union"]

[dependencies.target-lexicon]
version = "0.13.3"

[dev-dependencies.cranelift-codegen]
version = "0.132.0"
features = [
    "std",
    "unwind",
    "x86",
]
default-features = false

[dev-dependencies.env_logger]
version = "0.11.5"

[dev-dependencies.similar]
version = "2.7.0"

[lints.clippy]
allow_attributes_without_reason = "warn"
clone_on_copy = "warn"
extra_unused_type_parameters = "warn"
from_over_into = "warn"
manual_strip = "warn"
map_clone = "warn"
multiple_bound_locations = "warn"
redundant_field_names = "warn"
uninlined_format_args = "warn"
unnecessary_cast = "warn"
unnecessary_fallible_conversions = "warn"
unnecessary_mut_passed = "warn"
unnecessary_to_owned = "warn"
useless_conversion = "warn"

[lints.clippy.all]
level = "allow"
priority = -1

[lints.rust]
trivial_numeric_casts = "warn"
unstable_features = "warn"
unused-lifetimes = "warn"
unused-macro-rules = "warn"
unused_extern_crates = "warn"
unused_import_braces = "warn"

[lints.rust.unexpected_cfgs]
level = "warn"
priority = 0
check-cfg = [
    "cfg(pulley_tail_calls)",
    "cfg(pulley_assume_llvm_makes_tail_calls)",
    "cfg(pulley_disable_interp_simd)",
    "cfg(arc_try_new)",
    "cfg(fuzzing)",
    "cfg(gc_zeal)",
]
